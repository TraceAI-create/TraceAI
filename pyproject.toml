[project]
name = "traceai"
version = "0.1.0"
description = "AI-powered decision traceability and audit system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.116.0",
]

[tool.uv]
package = false